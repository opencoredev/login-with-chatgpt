<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Backups are disabled: the token store is Keystore-encrypted and the key
         never leaves the device, so a restored backup would be undecryptable. -->
    <application
        android:allowBackup="false"
        android:label="Login with ChatGPT"
        android:supportsRtl="true"
        android:theme="@style/Theme.LwcSample">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
